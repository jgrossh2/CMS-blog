<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
        <!-- same as format_date(created_at) -->
      {{user.username}} on {{createdAt}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>